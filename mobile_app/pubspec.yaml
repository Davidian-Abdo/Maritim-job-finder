name: maritime_jobs
description: Maritime Engineering Job Aggregator
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.1
  dio_smart_retry: ^7.0.1
  http: ^1.5.0
  flutter_secure_storage: ^10.0.0    # ✅ Null‑safe, Windows support
  provider: ^6.1.1
  pull_to_refresh: ^2.0.0
  url_launcher: ^6.1.10             # ✅ Added for external URLs

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true